# infra/

Placeholder para configurações de infraestrutura de produção (nginx com TLS,
manifests de deploy, pipelines de CI/CD). O detalhamento está em
[`docs/deployment.md`](../docs/deployment.md).

Na Fase 0, todo o ambiente de desenvolvimento sobe via `docker-compose.yml` na
raiz do projeto. Estes arquivos de produção serão adicionados nas fases
seguintes (ver `docs/roadmap.md`).
